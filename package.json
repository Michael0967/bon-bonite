{
  "name": "bon-bonite",
  "version": "1.0.0",
  "author": "Michael Rojas",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "npm run build && node --env-file-if-exists=.env ./node_modules/@cucumber/cucumber/bin/cucumber.js",
    "report": "node scripts/generate-report.mjs"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^13.2.1",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "multiple-cucumber-html-reporter": "^4.2.0",
    "typescript": "^5.9.3"
  }
}
